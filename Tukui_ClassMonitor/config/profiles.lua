local ADDON_NAME, Engine = ...
if not Engine.Enabled then return end

-- if Engine.MyName == "Meuhhnon" then
	-- Engine.Config["DRUID"][1].anchor = nil -- MOVER
	-- Engine.Config["DRUID"][1].anchors = {
		-- { "CENTER", UIParent, "CENTER", 0, -140 }, -- Balance
		-- { "CENTER", UIParent, "CENTER", 0, -140 }, -- Feral
		-- { "CENTER", UIParent, "CENTER", 0, -140 }, -- Guardian
		-- { "CENTER", UIParent, "CENTER", 0, -140 } -- Restoration
	-- }
-- end
-- if Engine.MyName == "Enimouchet" then
	-- Engine.Config["PALADIN"][1].anchor = nil -- MOVER
	-- Engine.Config["PALADIN"][1].anchors = {
		-- --{"CENTER", UIParent, "CENTER", -543, 290}, -- Holy
		-- { "CENTER", UIParent, "CENTER", 0, -140 }, -- Holy
		-- {"CENTER", UIParent, "CENTER", -0, -100}, -- Protection
		-- {"CENTER", UIParent, "CENTER", -0, -100} -- Retribution
	-- }
-- end
-- if Engine.MyName == "Nyara" or Engine.MyName == "Graougraou" then
	-- Engine.Config["ROGUE"][1].anchor = { "CENTER", UIParent, "CENTER", 0, -150 } -- MOVER
-- end